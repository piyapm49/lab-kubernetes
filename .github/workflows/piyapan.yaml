name: Lab-piyapan
on:
  push:
    branches:
      - main
env:
  WORDING: "PIYAPAN"

jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: check github environment
        run: hostname
      - name: create
        run: echo "Piyapan version 1.0.2" >> $GITHUB_ENV
      - name: build appilcation
        run: echo "hello ${{ env.WORDING }} by $GITHUB_ENV"

  deploy: 
    runs-on: piyapan
    steps:
      - name: check github environment
        run: hostname