name: Lab-piyapan
on:
  push:
    branches:
      - main
env:
  WORDING: "PIYAPAN"

jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: check github environment
        run: hostname
      - name: create
        run: echo "BUILD_ARGS=Piyapan version 1.0.2" >> $GITHUB_ENV
      - name: build appilcation
        run: echo "hello ${{ env.WORDING }} by $BUILD_ARGS"

  deploy: 
    runs-on: piyapan
    steps:
      - name: check github environment
        run: hostname